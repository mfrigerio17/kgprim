'''
Tests for the `motiondsl` package.
'''

# Do not name this package "motiondsl" to avoid clashes with the actual
# `motiondsl` package, which is a top level package (even though this package is
# under `test`, it would clash should test-discovery be configured to use
# `test/` as the starting folder).
